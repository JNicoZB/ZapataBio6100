# class 18 feb

## dentro del paréntesis se debe especificar una condición cuando usamos la función if, con el "curly braquet" {} debe ir una expresion... 
# block A if (condition) {expresion1} else {expresion2} 
# "True" block A expresion 1 block b (todo esto hacia abajo)
# "False" block A expresion 2 block b

# block A
## if (condition 1) {expresion1} else 
#    if (condition 2) {expresion2}    else (en este caso no se necesita usar el else)
# block B

# lazy evaluation no considera que sucede en el segundo bloque 
# condition 1 TRUE y condition 2 False    block A expresion 1 block B

# aqui no importaria si la expresion dos es diferente
#condition 1 TRUE conditio 2 TRUE   block A expression 1 block B

# condition 1 FALSE condition FALSE    block A bloc B
#



